function formatTime(iso) { if (!iso) return '--'; const d = new Date(iso); return d.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit', hour12: true }); }
function calcHours(checkIn, checkOut) { if (!checkIn) return '--'; if (!checkOut) return 'Active'; const diff = new Date(checkOut).getTime() - new Date(checkIn).getTime(); const hrs = Math.floor(diff / 3600000); const mins = Math.floor((diff % 3600000) / 60000); return `${hrs}h ${mins}m`; }
function mapApiToRecord(a) { const isTodayFormat = a.checkedIn !== undefined; return { id: a.id || a.userId || Math.random().toString(), employeeId: a.userId, name: isTodayFormat ? a.name : (a.user?.name || 'Unknown'), date: a.date ? new Date(a.date).toISOString().split('T')[0] : new Date().toISOString().split('T')[0], checkIn: formatTime(a.checkInTime), checkOut: formatTime(a.checkOutTime), hours: calcHours(a.checkInTime, a.checkOutTime), status: a.status?.toLowerCase() || (isTodayFormat && !a.checkedIn ? 'absent' : 'present'), location: '' }; }
console.log(mapApiToRecord({ userId: '123', name: 'Test', employeeId: '456', checkedIn: false, checkInTime: null, checkOutTime: null, status: 'ABSENT', isLate: false, selfieUrl: null }));
